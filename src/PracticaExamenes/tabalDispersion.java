package PracticaExamenes;

import java.util.HashSet;

/*public class tabalDispersion {
       public int compress(){
        Set<T> lista = new HashSet<>();
        int contador=0;
        for(int i = 0; i<table.size(); i++){
            T actual = table.get(i);
            if(actual!=null){
                if(lista.contains(actual)){
                    table.set(i, null);
                    size--;
                    contador++;
                }else{
                    lista.add(actual);
                }
            }
        }
        return contador;
    }
}
*/